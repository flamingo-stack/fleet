<!-- source-hash: 39ebe32c83276d8489c498228caa5e73 -->
Database migration that registers the `FLEET_VAR_HOST_PLATFORM` fleet variable in the `fleet_variables` table using an idempotent `INSERT IGNORE` statement.

## Key Components

- **`Up_20251124090450`** — Inserts the `FLEET_VAR_HOST_PLATFORM` entry into `fleet_variables` with a deterministic `created_at` timestamp (`2025-11-19 UTC`). Uses `INSERT IGNORE` to ensure the migration is safe to re-run.
- **`Down_20251124090450`** — No-op rollback; the variable is left in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is applied automatically via the Fleet migration runner. No manual invocation is required.

```go
// Migration is auto-registered on package import via init()
// and executed by the migration client in sequence order.

// The inserted row is equivalent to:
// INSERT IGNORE INTO fleet_variables (name, is_prefix, created_at)
// VALUES ('FLEET_VAR_HOST_PLATFORM', 0, '2025-11-19 00:00:00');
```

## Notes

- The `created_at` value is hardcoded to `2025-11-19T00:00:00Z` to keep the generated schema deterministic across environments.
- `is_prefix = 0` indicates this is an exact-match variable, not a prefix-based one.
- Source: [`20251124090450_AddHostPlatformFleetVar.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20251124090450_AddHostPlatformFleetVar.go)