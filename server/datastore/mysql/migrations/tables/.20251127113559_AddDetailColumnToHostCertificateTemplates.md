<!-- source-hash: 8ede4b2fecd79ddbf1d9871e47683bbc -->
Database migration that adds a nullable `detail` TEXT column to the `host_certificate_templates` table, using an idempotent check to prevent duplicate column errors.

## Key Components

| Function | Description |
|---|---|
| `Up_20251127113559` | Adds `detail TEXT` column (utf8mb4 charset) to `host_certificate_templates` if it doesn't already exist |
| `Down_20251127113559` | No-op rollback (column removal not implemented) |

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
func init() {
    MigrationClient.AddMigration(Up_20251127113559, Down_20251127113559)
}

// Column added to host_certificate_templates:
// detail TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL
```

The `columnExists` guard makes the migration safe to re-run — if `detail` already exists, the `ALTER TABLE` is skipped.

## Notes

- **Charset:** `utf8mb4` with `utf8mb4_unicode_ci` collation supports full Unicode including emoji
- **Nullable:** Column defaults to `NULL`, so existing rows are unaffected
- **Rollback:** `Down` is a no-op; manual intervention required to drop the column if needed

**Source:** [`20251127113559_AddDetailColumnToHostCertificateTemplates.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20251127113559_AddDetailColumnToHostCertificateTemplates.go)