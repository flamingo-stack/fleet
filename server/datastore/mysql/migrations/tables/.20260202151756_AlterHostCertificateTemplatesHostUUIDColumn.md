<!-- source-hash: 4eaa1f1d9335cd7462670b902d770634 -->
Alters the `host_uuid` column in the `host_certificate_templates` table to explicitly set the character set (`utf8mb4`) and collation (`utf8mb4_unicode_ci`).

## Key Components

- **`Up_20260202151756`** — Executes an `ALTER TABLE` statement to redefine `host_uuid` as `VARCHAR(255)` with `utf8mb4` character set and `utf8mb4_unicode_ci` collation, preserving the `NOT NULL` constraint. The migration is idempotent and safe to re-run.
- **`Down_20260202151756`** — No-op rollback; the column change does not require reversal.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration runs automatically via the Fleet migration client. To apply manually:

```bash
fleetctl prepare db --config /etc/fleet/fleet.yml
```

To verify the column definition after migration:

```sql
SHOW COLUMNS FROM host_certificate_templates WHERE Field = 'host_uuid';
```

Expected output:

```text
+-----------+--------------+------+-----+---------+-------+
| Field     | Type         | Null | Key | Default | Extra |
+-----------+--------------+------+-----+---------+-------+
| host_uuid | varchar(255) | NO   |     | NULL    |       |
+-----------+--------------+------+-----+---------+-------+
```

## Source

[`20260202151756_AlterHostCertificateTemplatesHostUUIDColumn.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20260202151756_AlterHostCertificateTemplatesHostUUIDColumn.go)