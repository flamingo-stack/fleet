<!-- source-hash: 6a4b768e696b9f2fc2c0a880cba12407 -->
Database migration that adds a `gigs_total_disk_space` column to the `host_disks` table to track total disk capacity per host.

## Key Components

- **`Up_20231219143041`** — Idempotent migration that adds `gigs_total_disk_space` as a `decimal(10,2)` column with a default value of `0.00` to `host_disks`, guarded by a `columnExists` check.
- **`Down_20231219143041`** — No-op rollback; the column addition is not reversed.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration runner at startup.
// Manual invocation via MigrationClient:
err := MigrationClient.Up(db, "20231219143041")
if err != nil {
    log.Fatalf("migration failed: %v", err)
}
```

> **Note:** The `Up` migration is safe to run multiple times — it checks for column existence before attempting the `ALTER TABLE`, preventing duplicate column errors.

## Schema Change

```text
host_disks
├── ...existing columns...
└── gigs_total_disk_space  decimal(10,2)  NOT NULL  DEFAULT '0.00'  ← added
```

**Source:** [`server/datastore/mysql/migrations/tables/20231219143041_AddGigsTotalToHostDisks.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20231219143041_AddGigsTotalToHostDisks.go)