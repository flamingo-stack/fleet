<!-- source-hash: 328e9ccace84f6ad80f8b5a6f8a86275 -->
Database migration that adds a `resolution` TEXT column to the `policies` table, with idempotent up migration and a no-op rollback.

## Key Components

- **`Up_20211013133706`** — Checks if the `resolution` column already exists on the `policies` table before applying the `ALTER TABLE` statement, ensuring safe re-runs.
- **`Down_20211013133706`** — No-op rollback; the column is not removed on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration client at startup.
// Manual invocation via sql.Tx:
err := Up_20211013133706(tx)
if err != nil {
    log.Fatal(err)
}
```

## Notes

- The migration is **idempotent**: calling `Up_20211013133706` multiple times is safe due to the `columnExists` guard.
- The `resolution` column stores free-form text, intended to hold remediation or resolution instructions for a policy.
- Rollback (`Down_20211013133706`) is intentionally a no-op — dropping the column was deemed unnecessary or unsafe for this migration.

## Source

[`20211013133706_AddResolutionColumnToPolicies.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20211013133706_AddResolutionColumnToPolicies.go)