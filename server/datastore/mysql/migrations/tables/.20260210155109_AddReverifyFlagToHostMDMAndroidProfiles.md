<!-- source-hash: ea6a631a2d97127c1cf43cb9dc38a46c -->
Database migration that adds a `can_reverify` boolean column to the `host_mdm_android_profiles` table.

## Key Components

- **`Up_20260210155109`** — Idempotent migration that adds the `can_reverify TINYINT(1) NOT NULL DEFAULT 0` column to `host_mdm_android_profiles` if it does not already exist.
- **`Down_20260210155109`** — No-op rollback; column removal is not implemented.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration runner at startup.
// Manual invocation example:
db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20260210155109(tx); err != nil {
    tx.Rollback()
    log.Fatal(err)
}
tx.Commit()
```

## Notes

- The migration is **idempotent**: it checks for column existence via `columnExists` before issuing the `ALTER TABLE`, making it safe to run multiple times.
- The `Down` function is intentionally a no-op; destructive rollbacks are not performed automatically.
- Source: [`20260210155109_AddReverifyFlagToHostMDMAndroidProfiles.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20260210155109_AddReverifyFlagToHostMDMAndroidProfiles.go)