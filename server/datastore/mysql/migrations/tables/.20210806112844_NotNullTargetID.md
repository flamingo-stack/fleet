<!-- source-hash: 94c6dde4ed1367065b17544e3e5bc374 -->
Database migration that enforces a `NOT NULL` constraint on the `target_id` column in the `pack_targets` table.

## Key Components

- **`Up_20210806112844`** — Removes any rows where `target_id IS NULL`, then alters the `pack_targets.target_id` column to `int unsigned NOT NULL`. The operation is idempotent: it checks column existence before attempting the `ALTER TABLE`.
- **`Down_20210806112844`** — No-op rollback; the constraint removal is not reversed.

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20210806112844, Down_20210806112844)
```

To apply manually during development:

```bash
fleet db prepare --config /path/to/config.yml
```

> **Note:** Any `pack_targets` rows with a `NULL` `target_id` are permanently deleted before the constraint is applied. Verify data integrity before running this migration in production.

## Source

[`20210806112844_NotNullTargetID.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20210806112844_NotNullTargetID.go)