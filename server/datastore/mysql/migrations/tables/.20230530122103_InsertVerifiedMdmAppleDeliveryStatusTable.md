<!-- source-hash: 389619bdbf33393d59297be3e8ba0075 -->
Database migration that inserts the `"verified"` delivery status value into the `mdm_apple_delivery_status` table using an idempotent `INSERT IGNORE` statement.

## Key Components

- **`Up_20230530122103`** — Inserts the `"verified"` status into `mdm_apple_delivery_status`; uses `INSERT IGNORE` to safely re-run without error.
- **`Down_20230530122103`** — No-op rollback; the inserted row is left in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration runner at startup.
// To run manually during development:
err := Up_20230530122103(tx)
if err != nil {
    log.Fatal(err)
}
```

## Notes

- The migration is **idempotent** — running it multiple times is safe due to `INSERT IGNORE`.
- The empty `Down_` function means this change is **not reversible** through the migration framework.
- Part of the Fleet MDM Apple profile delivery status tracking system.

**Source:** [`20230530122103_InsertVerifiedMdmAppleDeliveryStatusTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20230530122103_InsertVerifiedMdmAppleDeliveryStatusTable.go)