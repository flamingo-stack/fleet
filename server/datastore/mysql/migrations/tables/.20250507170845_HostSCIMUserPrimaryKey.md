<!-- source-hash: 847f829c4f62aa9565fb10936773bece -->
Database migration that changes the `host_scim_user` table's primary key from a composite `(host_id, scim_user_id)` to a single-column `(host_id)`, deduplicating rows by retaining only the earliest `scim_user_id` per host.

## Key Components

| Function | Description |
|---|---|
| `Up_20250507170845` | Idempotent migration that drops the composite PK, deduplicates rows (keeping lowest `scim_user_id` per `host_id`), and sets `host_id` as the sole primary key |
| `Down_20250507170845` | Reverts to the original composite `(host_id, scim_user_id)` primary key and restores the `fk_host_scim_scim_user_id` foreign key — **note: data removed during `Up` cannot be recovered** |

## Migration Steps (Up)

```text
1. Idempotency check — skips if scim_user_id is already absent from PRIMARY index
2. CREATE TEMPORARY TABLE with deduplicated rows (MIN scim_user_id per host_id)
3. DROP FOREIGN KEY fk_host_scim_scim_user_id + DROP PRIMARY KEY
4. DELETE all rows from host_scim_user
5. INSERT deduplicated rows back from temp table
6. ADD PRIMARY KEY (host_id) + re-ADD foreign key constraint
7. DROP TEMPORARY TABLE
```

## Usage Example

This file is auto-registered via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20250507170845, Down_20250507170845)

// Run via standard migration tooling, e.g.:
// fleet db migrate
```

## Notes

- The `Up` migration is **idempotent**: it checks `INFORMATION_SCHEMA.STATISTICS` before executing to avoid re-running on an already-migrated schema.
- Uses `fkExists()` (shared helper) to guard constraint drops/adds against duplicate or missing constraint errors.
- `Down` restores schema structure only; **deduplicated data is permanently lost**.

**Source:** [`db/migrations/tables/20250507170845_HostSCIMUserPrimaryKey.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20250507170845_HostSCIMUserPrimaryKey.go)