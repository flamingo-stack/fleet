<!-- source-hash: 71a98609254d2441244b2ca177b12963 -->
Database migration that creates the `query_results` table for storing per-host query execution results, including query data, errors, and fetch timestamps.

## Key Components

- **`Up_20231009094544`** — Creates the `query_results` table if it does not already exist. The migration is idempotent (`CREATE TABLE IF NOT EXISTS`).
- **`Down_20231009094544`** — No-op rollback; the table is not dropped on downgrade.

### `query_results` Table Schema

| Column | Type | Notes |
|---|---|---|
| `id` | `INT UNSIGNED` | Auto-increment primary key |
| `query_id` | `INT(10) UNSIGNED` | FK → `queries(id)` (CASCADE delete) |
| `host_id` | `INT(10) UNSIGNED` | References the reporting host |
| `osquery_version` | `VARCHAR(50)` | osquery agent version on the host |
| `error` | `TEXT` | Execution error, nullable |
| `last_fetched` | `TIMESTAMP` | When results were last retrieved |
| `data` | `JSON` | Raw query result payload |

Character set: `utf8mb4` / `utf8mb4_unicode_ci`.

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
func init() {
    MigrationClient.AddMigration(Up_20231009094544, Down_20231009094544)
}

// Run all pending migrations
err := MigrationClient.Up(db)
if err != nil {
    log.Fatal(err)
}
```

## Source

[`20231009094544_CreateTableQueryReports.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20231009094544_CreateTableQueryReports.go)