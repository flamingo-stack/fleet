<!-- source-hash: f0a451eab986211c7d8ac81bc5fc1d58 -->
Database migration that creates the `certificate_templates` table, linking certificate authorities to teams with named templates.

## Key Components

- **`Up_20251124140138`** — Creates the `certificate_templates` table (idempotent via `CREATE TABLE IF NOT EXISTS`)
- **`Down_20251124140138`** — No-op rollback function

## Table Schema

| Column | Type | Notes |
|--------|------|-------|
| `id` | `INT UNSIGNED` | Auto-increment primary key |
| `team_id` | `INT UNSIGNED` | FK → `teams(id)` |
| `certificate_authority_id` | `INT` | FK → `certificate_authorities(id)` |
| `name` | `VARCHAR(255)` | Template name, unique per team |
| `subject_name` | `TEXT` | Certificate subject |
| `created_at` / `updated_at` | `TIMESTAMP` | Auto-managed timestamps |

**Unique constraint:** `(team_id, name)` — prevents duplicate template names within the same team.

## Usage Example

```go
// Migration is registered automatically via init()
// and executed by MigrationClient during schema upgrades.

// Example: inserting a certificate template after migration
_, err := db.Exec(`
    INSERT INTO certificate_templates (team_id, certificate_authority_id, name, subject_name)
    VALUES (?, ?, ?, ?)`,
    1, 42, "Web Server TLS", "CN=example.com,O=Acme Corp",
)
```

## Source

[`20251124140138_CreateTableCertifcatesTemplates.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20251124140138_CreateTableCertifcatesTemplates.go)