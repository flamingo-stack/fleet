<!-- source-hash: df89957d811aa66c3d2a875fac9f1436 -->
Database migration that creates the `mdm_windows_enrollments` table for storing Windows MDM (Mobile Device Management) enrollment records.

## Key Components

- **`Up_20230629140530`** — Creates the `mdm_windows_enrollments` table with device identity, enrollment metadata, and audit timestamps. The migration is idempotent via `CREATE TABLE IF NOT EXISTS`.
- **`Down_20230629140530`** — Drops the `mdm_windows_enrollments` table via `DROP TABLE IF EXISTS`.
- **`init`** — Registers both migration functions with `MigrationClient`.

### Table Schema: `mdm_windows_enrollments`

| Column | Type | Notes |
|---|---|---|
| `id` | `INT UNSIGNED` | Auto-increment primary key |
| `mdm_device_id` | `VARCHAR(255)` | MDM-assigned device identifier |
| `mdm_hardware_id` | `VARCHAR(255)` | Hardware identifier; unique key |
| `device_state` | `VARCHAR(255)` | Current device state |
| `device_type` | `VARCHAR(255)` | Type classification of the device |
| `device_name` | `VARCHAR(255)` | Human-readable device name |
| `enroll_type` | `VARCHAR(255)` | Enrollment method/type |
| `enroll_user_id` | `VARCHAR(255)` | User who enrolled the device |
| `enroll_proto_version` | `VARCHAR(255)` | MDM protocol version |
| `enroll_client_version` | `VARCHAR(255)` | Client software version |
| `not_in_oobe` | `TINYINT(1)` | Whether device is past Out-Of-Box Experience |
| `created_at` | `TIMESTAMP` | Auto-set on insert |
| `updated_at` | `TIMESTAMP` | Auto-updated on change |

## Usage Example

```go
// Applied automatically by the migration runner
// To run manually:
err := MigrationClient.Up(db)
if err != nil {
    log.Fatal(err)
}

// To roll back:
err = MigrationClient.Down(db)
```

## Source

[`20230629140530_AddMDMWindowsEnrollmentsTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20230629140530_AddMDMWindowsEnrollmentsTable.go)