<!-- source-hash: 8de7ebde3f37be3c1d824c38c5e1c0bb -->
Database migration that adds a `resolved_in_version` column to the `software_cve` table, tracking the software version in which a CVE vulnerability was resolved.

## Key Components

- **`Up_20230918132351`** — Idempotent migration that adds a nullable `VARCHAR(255)` column `resolved_in_version` to the `software_cve` table using `utf8mb4_unicode_ci` collation. Skips execution if the column already exists.
- **`Down_20230918132351`** — No-op rollback; the column is not removed on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration client on startup.
// To run manually via sql.Tx:
func Up_20230918132351(tx *sql.Tx) error {
    if !columnExists(tx, "software_cve", "resolved_in_version") {
        stmt := `
            ALTER TABLE software_cve
            ADD COLUMN resolved_in_version VARCHAR(255)
            COLLATE utf8mb4_unicode_ci DEFAULT NULL;
        `
        if _, err := tx.Exec(stmt); err != nil {
            return fmt.Errorf("add resolved_in_version column to software_cve: %w", err)
        }
    }
    return nil
}
```

## Notes

- **Idempotent**: The `columnExists` guard ensures the migration is safe to re-run without errors.
- **Nullable by default**: `DEFAULT NULL` means existing rows are unaffected after the column is added.
- The empty `Down` function means this schema change is intentionally irreversible via the migration tool.

## Source

[`20230918132351_AddResolvedInVersionToSoftwareCVE.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20230918132351_AddResolvedInVersionToSoftwareCVE.go)