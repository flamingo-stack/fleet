<!-- source-hash: 3d4190ff71315003ac9850fc1d38b5e5 -->
Database migration that adds the `AppleRequireHardwareAttestation` field to the MDM section of the Fleet `AppConfig` JSON, initializing it to `false`.

## Key Components

- **`Up_20260401153001`** — Applies the migration by updating the stored `AppConfig` JSON, setting `MDM.AppleRequireHardwareAttestation` to `false`. The operation is idempotent and safe to re-run.
- **`Down_20260401153001`** — No-op rollback; the JSON config field requires no explicit removal.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is executed automatically by the Fleet migration runner. No manual invocation is required.

```go
// Registered automatically via init()
MigrationClient.AddMigration(Up_20260401153001, Down_20260401153001)

// Internally, Up applies:
config.MDM.AppleRequireHardwareAttestation = false
```

## Notes

- **Idempotent** — Uses `UPDATE`/JSON-config mutation only; safe to apply multiple times without side effects.
- **Rollback** — `Down_20260401153001` is intentionally empty; removing the field from JSON config is not necessary for a safe rollback.
- The migration targets the `AppleRequireHardwareAttestation` field introduced for Apple MDM hardware attestation enforcement in Fleet's app configuration.

**Source:** [`20260401153001_AddAppleRequireHardwareAttestationToAppConfig.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20260401153001_AddAppleRequireHardwareAttestationToAppConfig.go)