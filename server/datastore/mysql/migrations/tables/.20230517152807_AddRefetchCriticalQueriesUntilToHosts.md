<!-- source-hash: 5c01eddd08f31538cd452a721a9d6959 -->
Database migration that adds the `refetch_critical_queries_until` nullable timestamp column to the `hosts` table, enabling scheduled re-fetching of critical queries until a specified time.

## Key Components

- **`Up_20230517152807`** — Idempotent migration that adds `refetch_critical_queries_until TIMESTAMP NULL` to `hosts` if the column does not already exist
- **`Down_20230517152807`** — No-op rollback; column removal is not implemented
- **`init`** — Registers both migration functions with `MigrationClient`

## Usage Example

```go
// Migration is registered automatically via init().
// Run via the MigrationClient during application startup:
if err := MigrationClient.Up(db, "20230517152807"); err != nil {
    log.Fatal(err)
}
```

## Notes

- The `Up` migration is **idempotent** — it checks for column existence via `columnExists` before issuing the `ALTER TABLE`, making it safe to run multiple times without error.
- The `Down` migration is intentionally a no-op; reverting this schema change is not supported.
- Source: [`20230517152807_AddRefetchCriticalQueriesUntilToHosts.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20230517152807_AddRefetchCriticalQueriesUntilToHosts.go)