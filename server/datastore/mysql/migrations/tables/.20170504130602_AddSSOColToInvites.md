<!-- source-hash: 30b5989a209156d50e8bb19332de4327 -->
Adds an `sso_enabled` column to the `invites` table, enabling Single Sign-On support for user invitations.

## Key Components

- **`Up_20170504130602`** — Idempotent migration that adds a `sso_enabled TINYINT(1) NOT NULL DEFAULT FALSE` column after the `token` column in the `invites` table. Checks for column existence before executing to prevent duplicate column errors.
- **`Down_20170504130602`** — Rolls back the migration by dropping the `sso_enabled` column from the `invites` table.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is executed automatically by the migration runner. To apply or roll back manually:

```go
// Apply migration
err := Up_20170504130602(tx)
if err != nil {
    log.Fatal(err)
}

// Rollback migration
err = Down_20170504130602(tx)
if err != nil {
    log.Fatal(err)
}
```

**Resulting schema change:**

```text
invites
├── ...
├── token       VARCHAR
├── sso_enabled TINYINT(1) NOT NULL DEFAULT FALSE  ← added
└── ...
```

**Source:** [`20170504130602_AddSSOColToInvites.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20170504130602_AddSSOColToInvites.go)