<!-- source-hash: 70c89c56e04f0964f0f2570910162749 -->
Database migration that adds a `profile_uuid` column to the `mdm_apple_setup_assistants` table as part of Fleet MDM's Apple Setup Assistant functionality.

## Key Components

- **`Up_20230501154913`** — Adds a `profile_uuid VARCHAR(255) NOT NULL DEFAULT ''` column to `mdm_apple_setup_assistants`. The migration is idempotent; it checks for column existence before executing the `ALTER TABLE` statement.
- **`Down_20230501154913`** — No-op rollback; the column addition is not reversed.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is executed automatically by the Fleet migration runner. No manual invocation is required.

```go
// Migration is registered on package init and run via:
MigrationClient.AddMigration(Up_20230501154913, Down_20230501154913)
```

To apply manually during development:

```bash
# Run all pending migrations
fleetctl prepare db --config /path/to/config.yml
```

## Notes

- The idempotency check (`columnExists`) ensures safe re-execution in environments where migrations may be applied multiple times.
- The empty-string default (`DEFAULT ''`) means existing rows will have a blank `profile_uuid` until explicitly updated.
- The missing `Down` implementation means this schema change is effectively irreversible through the migration framework.

**Source:** [`20230501154913_AlterMDMAppleSetupAssistantsAddProfileUUID.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20230501154913_AlterMDMAppleSetupAssistantsAddProfileUUID.go)