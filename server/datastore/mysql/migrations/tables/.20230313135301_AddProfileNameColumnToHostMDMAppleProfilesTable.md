<!-- source-hash: 7ee0036c39eafd7ed1021b0fd849922e -->
Database migration that adds a `profile_name` column to the `host_mdm_apple_profiles` table for storing Apple MDM profile display names.

## Key Components

- **`Up_20230313135301`** — Idempotent migration that adds a `profile_name` VARCHAR(255) column (utf8mb4, non-null, default `''`) to `host_mdm_apple_profiles`. Uses `columnExists` to skip the ALTER if the column is already present.
- **`Down_20230313135301`** — No-op rollback; the column is not removed on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration client at startup.
// To run manually via goose or a migration runner:
MigrationClient.AddMigration(Up_20230313135301, Down_20230313135301)
```

The resulting column definition:

```sql
ALTER TABLE `host_mdm_apple_profiles`
  ADD COLUMN `profile_name` varchar(255)
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci
  NOT NULL DEFAULT '';
```

## Notes

- **Idempotent**: safe to run multiple times; checks for column existence before attempting the `ALTER TABLE`.
- **No rollback**: `Down_` is a no-op, so downgrading does not drop the column.
- Source: [`tables/20230313135301_AddProfileNameColumnToHostMDMAppleProfilesTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/tables/20230313135301_AddProfileNameColumnToHostMDMAppleProfilesTable.go)