<!-- source-hash: fd632b1477b371c3e987aa5487257f59 -->
Database migration that adds a `cancelled` enum value to the `status` column of the `setup_experience_status_results` table.

## Key Components

- **`Up_20250520153848`** — Alters the `status` column to include the `cancelled` value in its `ENUM` definition. The migration is idempotent, checking for column existence before executing the `ALTER TABLE` statement.
- **`Down_20250520153848`** — No-op rollback; the enum modification is not reversed.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is applied automatically via the migration client. No manual invocation is required.

```go
// Migration is registered on package init and run by the migration client:
MigrationClient.AddMigration(Up_20250520153848, Down_20250520153848)
```

After applying, the `status` column accepts the following values:

```text
ENUM('pending', 'running', 'success', 'failure', 'cancelled')
```

**Source:** [`server/datastore/mysql/migrations/tables/20250520153848_AddCancelledStatusToSetupExperienceStatusResults.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20250520153848_AddCancelledStatusToSetupExperienceStatusResults.go)