<!-- source-hash: 47abcac8157c8501cf00c7b63bbe7eb4 -->
Database migration that creates the `host_last_known_locations` table to store geographic coordinates for tracked hosts.

## Key Components

- **`Up_20260108200708`** — Creates the `host_last_known_locations` table with latitude/longitude decimal columns, automatic timestamps, and `host_id` as the primary key. The `CREATE TABLE IF NOT EXISTS` clause makes this migration idempotent.
- **`Down_20260108200708`** — No-op rollback; the table is not dropped on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Schema Created

| Column | Type | Notes |
|---|---|---|
| `host_id` | `INT UNSIGNED` | Primary key |
| `latitude` | `DECIMAL(10,8)` | Up to ±90.00000000 |
| `longitude` | `DECIMAL(11,8)` | Up to ±180.00000000 |
| `created_at` | `TIMESTAMP(6)` | Auto-set on insert |
| `updated_at` | `TIMESTAMP(6)` | Auto-updated on row change |

## Usage Example

This migration runs automatically via the `MigrationClient` on schema upgrade:

```go
// Triggered by the migration runner — no manual invocation needed.
// To apply manually in a test or script:
db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20260108200708(tx); err != nil {
    tx.Rollback()
    log.Fatal(err)
}
tx.Commit()
```

## Source

[`20260108200708_AddHostLocationSupport.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20260108200708_AddHostLocationSupport.go)