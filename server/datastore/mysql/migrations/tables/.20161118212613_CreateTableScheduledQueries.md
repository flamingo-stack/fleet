<!-- source-hash: b00eb13fb6c3c7a3f84622e9fabb89dd -->
Database migration file that creates the `scheduled_queries` table, which associates osquery queries with packs for scheduled execution.

## Key Components

- **`Up_20161118212613`** — Creates the `scheduled_queries` table with columns for pack/query association, scheduling interval, and execution options. Migration is idempotent via `CREATE TABLE IF NOT EXISTS`.
- **`Down_20161118212613`** — Drops the `scheduled_queries` table via `DROP TABLE IF EXISTS`.
- **`init()`** — Registers both migration functions with `MigrationClient`.

### Table Schema

| Column | Type | Description |
|---|---|---|
| `id` | `int unsigned` | Auto-increment primary key |
| `pack_id` | `int unsigned` | Foreign key reference to a query pack |
| `query_id` | `int unsigned` | Foreign key reference to a saved query |
| `interval` | `int unsigned` | Execution interval in seconds |
| `snapshot` | `tinyint(1)` | Whether to run in snapshot mode |
| `removed` | `tinyint(1)` | Whether to log removed rows |
| `platform` | `varchar(255)` | Target platform filter |
| `version` | `varchar(255)` | Minimum osquery version filter |
| `shard` | `int unsigned` | Percentage of hosts to target |
| `deleted_at` / `deleted` | timestamp / tinyint | Soft-delete fields |

## Usage Example

```go
// Applied automatically by the migration runner
// To apply manually:
db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20161118212613(tx); err != nil {
    tx.Rollback()
}
tx.Commit()
```

## Source

[`20161118212613_CreateTableScheduledQueries.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20161118212613_CreateTableScheduledQueries.go)