<!-- source-hash: 82bf604b224c9ed39ac8eb741c7799e3 -->
Database migration that adds a nullable `subject_alternative_name` TEXT column to the `certificate_templates` table, supporting UTF-8 (utf8mb4) character encoding.

## Key Components

| Function | Description |
|---|---|
| `Up_20260522195230` | Idempotent migration that adds `subject_alternative_name` column if it does not already exist |
| `Down_20260522195230` | No-op rollback (column removal is not performed) |

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20260522195230, Down_20260522195230)
```

The column added:

```sql
ALTER TABLE certificate_templates
ADD COLUMN subject_alternative_name TEXT
CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL
```

## Notes

- **Idempotent**: Uses `columnExists()` guard — safe to run multiple times without error.
- **Rollback**: `Down` is a no-op; dropping the column must be handled manually if needed.
- **Encoding**: Column uses `utf8mb4_unicode_ci`, supporting full Unicode including emoji and multi-byte characters — appropriate for SAN values such as DNS names and email addresses.

**Source:** [`tables/20260522195230_AddSubjectAlternativeNameToCertificateTemplates.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/tables/20260522195230_AddSubjectAlternativeNameToCertificateTemplates.go)