<!-- source-hash: 085274956a481c6a09adb7aa2cbc7c6e -->
Adds a `variables_updated_at` column to the `host_mdm_apple_profiles` table via an idempotent database migration.

## Key Components

- **`Up_20250502154517`** — Applies the migration by adding a nullable `DATETIME(6)` column `variables_updated_at` to `host_mdm_apple_profiles`. Checks for column existence before altering, making it safe to run multiple times.
- **`Down_20250502154517`** — No-op rollback; the migration is intentionally non-reversible.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is applied automatically by the Fleet migration runner:

```bash
fleetctl prepare db --config /etc/fleet/fleet.yml
```

To run programmatically in tests or tooling:

```go
import "database/sql"

db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20250502154517(tx); err != nil {
    tx.Rollback()
    log.Fatal(err)
}
tx.Commit()
```

**Resulting schema change:**

```text
ALTER TABLE host_mdm_apple_profiles
  ADD COLUMN variables_updated_at DATETIME(6) NULL
```

## Notes

- **Idempotent:** Uses `columnExists` to skip the `ALTER TABLE` if the column is already present — safe for repeated runs or environments where the column was added out-of-band.
- **Source:** [`tables/20250502154517_AddVariablesUpdatedAtToHMAP.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20250502154517_AddVariablesUpdatedAtToHMAP.go)