<!-- source-hash: 5f8ef3a42caed529d259b6ecfb51eac6 -->
Database migration that creates the `software_title_display_names` table, enabling per-team custom display names for software titles.

## Key Components

- **`Up_20251103160848`** — Creates the `software_title_display_names` table with the following schema:
  - `id` — Auto-incrementing primary key
  - `team_id` — Team identifier (unsigned int, required)
  - `software_title_id` — Foreign key referencing `software_titles(id)` with cascade on delete/update
  - `display_name` — UTF-8 display name string (up to 255 chars, defaults to empty string)
  - `created_at` — Microsecond-precision timestamp
  - Unique constraint on `(team_id, software_title_id)` to enforce one display name per software title per team
- **`Down_20251103160848`** — No-op rollback (table removal not implemented)
- **`init`** — Registers both migration functions with `MigrationClient`

## Usage Example

This migration runs automatically via the Fleet migration client. To apply manually:

```go
db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20251103160848(tx); err != nil {
    tx.Rollback()
    log.Fatal(err)
}

tx.Commit()
```

## Notes

- Migration is **idempotent** — uses `CREATE TABLE IF NOT EXISTS`, safe to re-run
- Scoped to the `tables` package; registered automatically on import via `init()`
- Source: [`20251103160848_AddDisplayNameToSoftwareTitles.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20251103160848_AddDisplayNameToSoftwareTitles.go)