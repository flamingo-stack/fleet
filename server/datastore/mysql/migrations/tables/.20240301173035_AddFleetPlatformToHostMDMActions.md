<!-- source-hash: 72b2f5d12de8de09f1ec4e65ad5484d9 -->
Database migration that adds a `fleet_platform` column to the `host_mdm_actions` table.

## Key Components

- **`Up_20240301173035`** — Idempotent migration that adds a `fleet_platform` VARCHAR(255) column (UTF8MB4, NOT NULL, default `''`) to `host_mdm_actions` if it does not already exist.
- **`Down_20240301173035`** — No-op rollback; the column is not removed on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration runner at startup.
// To run manually using goose or the Fleet migration client:
MigrationClient.AddMigration(Up_20240301173035, Down_20240301173035)
```

The `columnExists` guard makes this migration safe to re-run:

```go
if !columnExists(tx, "host_mdm_actions", "fleet_platform") {
    // ALTER TABLE only executes when the column is absent
}
```

## Notes

- **Idempotent** — Checks for column existence before altering; safe for repeated runs or partial deployments.
- **No rollback** — `Down_20240301173035` is a no-op; dropping the column must be handled manually if needed.
- Column collation (`utf8mb4_unicode_ci`) is consistent with Fleet's standard MySQL schema conventions.

**Source:** [`20240301173035_AddFleetPlatformToHostMDMActions.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20240301173035_AddFleetPlatformToHostMDMActions.go)