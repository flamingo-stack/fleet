<!-- source-hash: fa507133796eebc911daf0af0707b73c -->
Database migration that adds a `team_id` column to the `android_devices` table, preserving team assignment data even after host record deletion.

## Key Components

- **`Up_20260604221206`** — Idempotent migration that:
  - Adds `team_id INT UNSIGNED DEFAULT NULL` column to `android_devices` (if not present)
  - Creates foreign key constraint `fk_android_devices_team_id` referencing `teams(id)` with `ON DELETE SET NULL`
  - Handles partial state (column exists but constraint missing) gracefully
  - Backfills `team_id` from the joined `hosts` table
- **`Down_20260604221206`** — No-op rollback (column is left in place)

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20260604221206, Down_20260604221206)
```

The migration handles three states idempotently:

```text
Case 1: Column missing         → ADD COLUMN + ADD CONSTRAINT + backfill
Case 2: Column exists, no FK   → ADD CONSTRAINT only + backfill
Case 3: Column + FK exist      → backfill only
```

After the migration, `android_devices.team_id` reflects the device's last-known team assignment, surviving `hosts` row deletions (FK sets it to `NULL` rather than cascading a delete).

**Source:** [`server/datastore/mysql/migrations/tables/20260604221206_AddTeamIDToAndroidDevices.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20260604221206_AddTeamIDToAndroidDevices.go)