<!-- source-hash: e7f59f991626da76110063b77870b5af -->
Creates the `android_enterprises` database table via an idempotent up/down migration pair registered with the migration client.

## Key Components

- **`Up_20250213104005`** — Creates the `android_enterprises` table with `IF NOT EXISTS` to ensure idempotency. Defines columns for `id` (auto-increment PK), `signup_name`, `enterprise_id` (both `utf8mb4_unicode_ci`), and `created_at`/`updated_at` timestamps with microsecond precision.
- **`Down_20250213104005`** — No-op rollback; the table is intentionally left in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient` at package load time.

## Usage Example

This migration runs automatically via the migration client. To apply or roll back manually:

```go
// Apply migration
db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20250213104005(tx); err != nil {
    tx.Rollback()
    log.Fatal(err)
}
tx.Commit()

// Rollback (no-op for this migration)
if err := Down_20250213104005(tx); err != nil {
    log.Fatal(err)
}
```

**Resulting schema:**

```text
android_enterprises
├── id          INT UNSIGNED AUTO_INCREMENT PRIMARY KEY
├── signup_name VARCHAR(63) utf8mb4_unicode_ci DEFAULT ''
├── enterprise_id VARCHAR(63) utf8mb4_unicode_ci DEFAULT ''
├── created_at  DATETIME(6) DEFAULT NOW(6)
└── updated_at  DATETIME(6) DEFAULT NOW(6) ON UPDATE NOW(6)
```

## Notes

- Migration timestamp: `20250213104005` (Feb 13, 2025)
- The down migration is intentionally a no-op — dropping the table on rollback could cause data loss.
- Source: [`20250213104005_AddAndroidEnterpriseTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20250213104005_AddAndroidEnterpriseTable.go)