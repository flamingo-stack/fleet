<!-- source-hash: d54563ce8d1c71a83c91bbdd7b5a128e -->
Database migration that creates the `user_api_endpoints` table for storing per-user API endpoint permissions, introduced in migration `20260409153714`.

## Key Components

- **`Up_20260409153714`** — Creates the `user_api_endpoints` table if it does not already exist, with a composite primary key on `(user_id, path, method)` and a cascading foreign key referencing `users(id)`.
- **`Down_20260409153714`** — No-op rollback; the table is not dropped on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

### Table Schema: `user_api_endpoints`

| Column | Type | Notes |
|---|---|---|
| `user_id` | `INT UNSIGNED` | FK → `users(id)`, cascades on delete |
| `path` | `VARCHAR(255)` | API endpoint path (utf8mb4) |
| `method` | `VARCHAR(10)` | HTTP method (utf8mb4) |
| `created_at` | `TIMESTAMP` | Defaults to current timestamp |

**Primary Key:** `(user_id, path, method)`

## Usage Example

This migration runs automatically via the migration client at startup. To apply or roll back manually:

```go
// Apply migration
err := Up_20260409153714(tx)
if err != nil {
    log.Fatalf("migration failed: %v", err)
}

// Rollback (no-op)
err = Down_20260409153714(tx)
```

## Notes

- The `Down` function is intentionally a no-op — dropping this table on rollback was not deemed safe, likely due to potential data loss concerns.
- The idempotent `tableExists` guard makes the `Up` function safe to run multiple times.

**Source:** [`20260409153714_AddApiEndpointPermissionsTables.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20260409153714_AddApiEndpointPermissionsTables.go)