<!-- source-hash: 0f74637ec5c3b34b332e32ab5e3273d5 -->
Database migration that creates the `software_cpe` table, linking software records to their Common Platform Enumeration (CPE) identifiers for vulnerability tracking.

## Key Components

- **`Up_20210721171531`** — Creates the `software_cpe` table with a foreign key reference to the `software` table (cascade on delete). The migration is idempotent via `CREATE TABLE IF NOT EXISTS`.
- **`Down_20210721171531`** — No-op rollback; table removal is not implemented.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Schema

| Column | Type | Notes |
|---|---|---|
| `id` | `int(10) unsigned` | Auto-increment primary key |
| `software_id` | `bigint unsigned` | FK → `software(id)`, cascades on delete |
| `created_at` | `timestamp` | Defaults to `CURRENT_TIMESTAMP` |
| `updated_at` | `timestamp` | Auto-updates on row change |
| `cpe` | `varchar(255)` | CPE string, required |

## Usage Example

This migration runs automatically via the migration client on startup or when applying schema changes:

```go
// Applied automatically; or invoked directly in tests:
err := Up_20210721171531(tx)
if err != nil {
    log.Fatal(err)
}
```

## Source

[`20210721171531_AddCPETable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20210721171531_AddCPETable.go)