<!-- source-hash: 94079d1d33a1393ac176e5f352d65e71 -->
Database migration that adds a `patch_query` TEXT column to the `software_installers` table using an idempotent up/down migration pattern.

## Key Components

- **`Up_20260324161944`** – Adds `patch_query TEXT COLLATE utf8mb4_unicode_ci NOT NULL` to `software_installers` if it does not already exist, using `columnExists` for idempotency
- **`Down_20260324161944`** – No-op rollback; column removal is not implemented
- **`init`** – Registers both migration functions with `MigrationClient`

## Usage Example

```go
// Migration is registered automatically via init().
// Run via the Fleet migration runner, e.g.:
//
//   fleetctl prepare db --config /etc/fleet/fleet.yml
//
// The migration guards against re-application:
if !columnExists(tx, "software_installers", "patch_query") {
    // ALTER TABLE only executes on first run
}
```

## Notes

- **Idempotent**: Safe to run multiple times; the `columnExists` check prevents duplicate column errors
- **Collation**: Uses `utf8mb4_unicode_ci` for full Unicode support including emoji
- **No rollback**: `Down_20260324161944` is intentionally empty — dropping the column must be handled manually if needed

## Source

[`20260324161944_AddPatchQueryColumn.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20260324161944_AddPatchQueryColumn.go)