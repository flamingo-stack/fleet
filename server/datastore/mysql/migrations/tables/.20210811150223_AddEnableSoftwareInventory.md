<!-- source-hash: befed3a4206e13c1f6ec0f44965f555a -->
Database migration that adds the `enable_software_inventory` boolean column to the `app_configs` table, controlling whether software inventory collection is enabled globally.

## Key Components

- **`Up_20210811150223`** — Adds `enable_software_inventory BOOL DEFAULT FALSE` to `app_configs`. The migration is idempotent; it checks for column existence before executing the `ALTER TABLE` statement.
- **`Down_20210811150223`** — No-op rollback; the column is left in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration runner.
// To trigger manually during development:
MigrationClient.AddMigration(Up_20210811150223, Down_20210811150223)

// Resulting schema change:
// ALTER TABLE app_configs
//   ADD COLUMN enable_software_inventory BOOL DEFAULT FALSE;
```

## Notes

- New deployments default to `enable_software_inventory = false`, meaning software inventory is **opt-in**.
- The idempotency guard (`columnExists` check) makes it safe to re-run this migration without errors.
- The empty `Down` function means this migration is **not reversible** — dropping the column must be handled manually if needed.

**Source:** [`20210811150223_AddEnableSoftwareInventory.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20210811150223_AddEnableSoftwareInventory.go)