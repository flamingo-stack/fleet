<!-- source-hash: 5fc6ff2c553883e55d373638fdd9f43d -->
Idempotent database migration that adds a `host_uuid` column to the `mdm_windows_enrollments` table.

## Key Components

- **`Up_20231025160156`** — Adds a `host_uuid` column (`varchar(255)`, `utf8mb4_unicode_ci`, `NOT NULL DEFAULT ''`) to `mdm_windows_enrollments`. Checks for column existence before altering to ensure idempotency.
- **`Down_20231025160156`** — No-op rollback; column removal is not implemented.
- **`init`** — Registers the migration with `MigrationClient`.

## Usage Example

```go
// Migration is auto-registered via init() and executed by the migration client.
// To apply:
//   fleet db migrate

// The migration guard prevents duplicate column errors on re-run:
if !columnExists(tx, "mdm_windows_enrollments", "host_uuid") {
    // ALTER TABLE executes only when column is absent
}
```

## Notes

- Safe to re-run; `columnExists` prevents duplicate `ALTER TABLE` errors.
- Downward migration is intentionally a no-op — dropping the column is not handled automatically.
- Source: [`20231025160156_AddHostUuidColumnToMdmWindowsEnrollments.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20231025160156_AddHostUuidColumnToMdmWindowsEnrollments.go)