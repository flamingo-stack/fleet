<!-- source-hash: cb924c164db9554ca30665841606f36e -->
Database migration that creates the `host_dep_assignments` table for tracking Device Enrollment Program (DEP) host assignments.

## Key Components

- **`Up_20230520153236`** — Creates the `host_dep_assignments` table with `host_id` (primary key), `added_at`, and `deleted_at` columns. Uses `CREATE TABLE IF NOT EXISTS` for idempotent execution.
- **`Down_20230520153236`** — No-op rollback; table removal is not implemented.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Schema

```text
host_dep_assignments
├── host_id    INT(10) UNSIGNED  NOT NULL  (PRIMARY KEY)
├── added_at   TIMESTAMP         NOT NULL  DEFAULT CURRENT_TIMESTAMP
└── deleted_at TIMESTAMP         NULL
```

## Usage Example

This migration runs automatically via the migration client. To apply manually:

```go
db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20230520153236(tx); err != nil {
    tx.Rollback()
    log.Fatal(err)
}
tx.Commit()
```

## Notes

- The `deleted_at` nullable column supports soft-delete patterns for DEP assignment tracking.
- Source: [`20230520153236_AddHostDepAssignmentsTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20230520153236_AddHostDepAssignmentsTable.go)