<!-- source-hash: cd9d2c09b4032ed9337d44fe62ffd16c -->
Database migration that adds the `FLEET_VAR_SCEP_WINDOWS_CERTIFICATE_ID` fleet variable to the `fleet_variables` table, enabling SCEP-based Windows certificate management via Fleet's variable system.

## Key Components

- **`Up_20251028140110`** — Inserts the `FLEET_VAR_SCEP_WINDOWS_CERTIFICATE_ID` record into `fleet_variables` using `INSERT IGNORE` for idempotency. Uses a fixed timestamp (`2025-10-22 UTC`) to ensure deterministic schema generation.
- **`Down_20251028140110`** — No-op rollback; the variable insertion is considered safe to leave in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration runs automatically via the Fleet migration client. No manual invocation is required. To apply manually during development:

```go
// Migrations are registered in init() and executed by the migration runner.
// To run all pending migrations:
MigrationClient.Up(db)

// The inserted row equivalent:
// INSERT IGNORE INTO fleet_variables (name, is_prefix, created_at)
// VALUES ('FLEET_VAR_SCEP_WINDOWS_CERTIFICATE_ID', 0, '2025-10-22 00:00:00')
```

## Notes

- Uses `INSERT IGNORE` — safe to re-run; duplicate key conflicts are silently skipped.
- The fixed `createdAt` timestamp (`2025-10-22 00:00:00 UTC`) ensures the generated schema SQL is deterministic across environments.
- Source: [`server/datastore/mysql/migrations/tables/20251028140110_AddSCEPWindowsCertificateIdFleetVariable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20251028140110_AddSCEPWindowsCertificateIdFleetVariable.go)