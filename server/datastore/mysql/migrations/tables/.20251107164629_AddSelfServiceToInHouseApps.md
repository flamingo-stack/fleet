<!-- source-hash: 95b6a3498538a7899a582402213cecbb -->
Database migration that adds a `self_service` boolean column to the `in_house_apps` and `host_in_house_software_installs` tables.

## Key Components

- **`Up_20251107164629`** — Idempotent migration that adds a `self_service tinyint(1) NOT NULL DEFAULT '0'` column to both `in_house_apps` and `host_in_house_software_installs`, guarded by `columnExists` checks.
- **`Down_20251107164629`** — No-op rollback; columns are not removed on downgrade.
- **`init`** — Registers the migration pair with `MigrationClient`.

## Usage Example

This migration runs automatically via the Fleet migration client. To apply manually:

```bash
fleet prepare db --config /etc/fleet/fleet.yml
```

To invoke programmatically in tests:

```go
tx, _ := db.Begin()
if err := Up_20251107164629(tx); err != nil {
    log.Fatal(err)
}
tx.Commit()
```

## Notes

- The migration is **idempotent** — safe to run multiple times; each `ALTER TABLE` is skipped if the column already exists.
- The `Down` function is intentionally empty; destructive rollbacks (dropping columns) are avoided to prevent data loss.
- Source: [`20251107164629_AddSelfServiceToInHouseApps.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20251107164629_AddSelfServiceToInHouseApps.go)