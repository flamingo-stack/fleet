<!-- source-hash: da4cb82e210fa56c871399394069e4cd -->
Adds a database index on the `created_at` column of the `activities` table to improve query performance for time-based lookups.

## Key Components

- **`Up_20230525151159`** — Creates the `activities_created_at_idx` index on `activities.created_at`. The migration is idempotent; it checks for index existence before attempting creation.
- **`Down_20230525151159`** — No-op rollback; the index is left in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is applied automatically via the migration client. No manual invocation is required.

```go
// Migration is registered on package init and run by the migration runner:
MigrationClient.AddMigration(Up_20230525151159, Down_20230525151159)

// The Up function guards against duplicate index creation:
if !indexExistsTx(tx, "activities", "activities_created_at_idx") {
    tx.Exec("CREATE INDEX activities_created_at_idx ON activities (created_at);")
}
```

## Notes

- **Idempotent** — Safe to run multiple times; skips index creation if it already exists.
- **No rollback** — `Down` is intentionally a no-op to avoid dropping an index that may have existed prior to this migration.

**Source:** [`20230525151159_AddCreatedAtIdxToActivities.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20230525151159_AddCreatedAtIdxToActivities.go)