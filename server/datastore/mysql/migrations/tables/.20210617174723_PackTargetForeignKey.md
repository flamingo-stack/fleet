<!-- source-hash: 011dd0b4620c8fbb307069680915e072 -->
Database migration that cleans up orphaned `pack_targets` rows and adds a cascading foreign key constraint from `pack_targets.pack_id` to `packs.id`.

## Key Components

- **`Up_20210617174723`** — Runs the forward migration in two idempotent steps:
  1. Deletes any `pack_targets` rows whose `pack_id` no longer references a valid `packs` record.
  2. Adds a `FOREIGN KEY (pack_id) REFERENCES packs (id) ON UPDATE CASCADE ON DELETE CASCADE` constraint (named `pack_targets_ibfk_1`) only if it does not already exist, using the `fkExists` helper.
- **`Down_20210617174723`** — No-op rollback; the constraint and deleted rows are not restored.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This file is auto-registered via `init()` and executed by the migration runner. No direct invocation is needed.

```go
// Migration is registered automatically on package import.
// To apply all pending migrations:
if err := MigrationClient.Up(db, nil); err != nil {
    log.Fatal("migration failed:", err)
}
```

## Notes

- The migration is **idempotent**: re-running it is safe because orphan deletion is a no-op when no orphans exist, and `fkExists` guards the `ALTER TABLE` statement.
- The `Down` function is intentionally empty — deleted orphan rows are unrecoverable and the FK drop was deemed unnecessary for rollback.

**Source:** [`server/datastore/mysql/migrations/tables/20210617174723_PackTargetForeignKey.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20210617174723_PackTargetForeignKey.go)