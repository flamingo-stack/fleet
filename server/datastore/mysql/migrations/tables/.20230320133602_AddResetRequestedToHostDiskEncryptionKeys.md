<!-- source-hash: b507293c79478c1b44f801f032279b10 -->
Database migration that adds a `reset_requested` boolean column to the `host_disk_encryption_keys` table, enabling tracking of disk encryption key reset requests per host.

## Key Components

| Function | Description |
|---|---|
| `Up_20230320133602` | Adds `reset_requested TINYINT(1) NOT NULL DEFAULT 0` column to `host_disk_encryption_keys` (idempotent — skips if column exists) |
| `Down_20230320133602` | No-op rollback; column removal is not performed |

## Usage Example

This migration is auto-registered via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20230320133602, Down_20230320133602)

// Column added to host_disk_encryption_keys:
// ALTER TABLE host_disk_encryption_keys
//   ADD COLUMN reset_requested TINYINT(1) NOT NULL DEFAULT 0
```

## Notes

- **Idempotent**: Uses `columnExists()` guard before altering the table, making it safe to re-run without error.
- **No rollback**: `Down_20230320133602` is intentionally a no-op — dropped columns risk data loss, so rollback is left as a manual operation.
- The `reset_requested` flag defaults to `0` (false), meaning no reset is pending unless explicitly set.

## Source

[`20230320133602_AddResetRequestedToHostDiskEncryptionKeys.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20230320133602_AddResetRequestedToHostDiskEncryptionKeys.go)