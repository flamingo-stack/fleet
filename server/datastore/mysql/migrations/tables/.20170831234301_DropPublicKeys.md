<!-- source-hash: 515a134f7c8b2ce3954469b8e4d015a2 -->
Database migration that drops the `public_keys` table (migration ID: `20170831234301`).

## Key Components

- **`Up_20170831234301`** — Idempotently drops the `public_keys` table using `DROP TABLE IF EXISTS`, ensuring the migration is safe to run even if the table does not exist.
- **`Down_20170831234301`** — Rolls back by delegating to `Down_20170131232841`, which recreates the `public_keys` table to its prior state.

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20170831234301, Down_20170831234301)
```

To apply or roll back manually during development:

```bash
# Apply migration
fleet db prepare --dev

# Rollback
fleet db prepare --dev --reset
```

> **Source:** [`server/datastore/mysql/migrations/tables/20170831234301_DropPublicKeys.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20170831234301_DropPublicKeys.go)