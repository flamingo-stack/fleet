<!-- source-hash: 627e31206b518166165ed1eff75bbfca -->
Database migration that creates the `ca_config_assets` table for storing Certificate Authority configuration assets and adds a `not_valid_after` column to the `host_mdm_managed_certificates` table.

## Key Components

- **`Up_20250304162702`** — Applies the migration:
  - Creates the `ca_config_assets` table (idempotent via `CREATE TABLE IF NOT EXISTS`) with support for `digicert` and `custom_scep_proxy` CA types
  - Conditionally adds `not_valid_after DATETIME(6)` column to `host_mdm_managed_certificates` if it does not already exist
- **`Down_20250304162702`** — No-op rollback; schema changes are not reversed
- **`init`** — Registers both migration functions with `MigrationClient`

## Schema Details

**`ca_config_assets`**

| Column | Type | Notes |
|---|---|---|
| `id` | `INT UNSIGNED` | Auto-increment primary key |
| `type` | `ENUM` | `digicert` or `custom_scep_proxy` |
| `name` | `VARCHAR(255)` | Unique index |
| `value` | `BLOB` | Raw CA asset data |
| `created_at` / `updated_at` | `DATETIME(6)` | Auto-managed timestamps |

**`host_mdm_managed_certificates`** — adds `not_valid_after DATETIME(6) NULL`

## Usage Example

```go
// Migration is auto-registered via init(); no manual invocation needed.
// MigrationClient applies it during the standard migration run:
err := MigrationClient.Up(db)
if err != nil {
    log.Fatal(err)
}
```

## Source

[`20250304162702_AddCATables.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20250304162702_AddCATables.go)