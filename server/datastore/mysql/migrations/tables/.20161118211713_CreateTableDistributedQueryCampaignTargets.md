<!-- source-hash: 78cefa9162cd95e489f6f43f049553ac -->
Database migration that creates the `distributed_query_campaign_targets` table, which maps distributed query campaigns to their targets (hosts or groups).

## Key Components

- **`Up_20161118211713`** — Creates the `distributed_query_campaign_targets` table with `IF NOT EXISTS` (idempotent). Columns include `id`, `type`, `distributed_query_campaign_id`, and `target_id`.
- **`Down_20161118211713`** — Drops the `distributed_query_campaign_targets` table if it exists, reverting the migration.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Schema

| Column | Type | Description |
|---|---|---|
| `id` | `INT UNSIGNED` | Auto-increment primary key |
| `type` | `INT` | Target type identifier (e.g., host, label) |
| `distributed_query_campaign_id` | `INT UNSIGNED` | FK reference to the parent campaign |
| `target_id` | `INT UNSIGNED` | FK reference to the resolved target |

## Usage Example

This migration is applied automatically by the migration client at startup. To run manually:

```go
db, _ := sql.Open("mysql", dsn)
tx, _ := db.Begin()

if err := Up_20161118211713(tx); err != nil {
    tx.Rollback()
    log.Fatal(err)
}
tx.Commit()
```

## Source

[`20161118211713_CreateTableDistributedQueryCampaignTargets.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20161118211713_CreateTableDistributedQueryCampaignTargets.go)