<!-- source-hash: c1f0570cac03990926634c4e73462841 -->
Database migration that creates the `setup_experience_software_installers` table, linking software installers to device setup experience workflows by platform and team scope.

## Key Components

- **`Up_20260615135619`** — Creates the `setup_experience_software_installers` table with a composite primary key on `(software_installer_id, platform)`, a team/platform index, and a cascading foreign key to `software_installers(id)`. The migration is idempotent via `CREATE TABLE IF NOT EXISTS`.
- **`Down_20260615135619`** — No-op rollback; table removal is left as a manual step.
- **`init()`** — Registers both migration functions with `MigrationClient`.

## Table Schema

| Column | Type | Notes |
|---|---|---|
| `software_installer_id` | `INT UNSIGNED` | FK → `software_installers(id)`, cascades on delete |
| `platform` | `VARCHAR(32)` | e.g. `darwin`, `windows`, `linux` |
| `global_or_team_id` | `INT UNSIGNED` | `0` for global, team ID otherwise |
| `created_at` | `TIMESTAMP` | Defaults to current timestamp |

## Usage Example

This migration is applied automatically by the Fleet migration runner. To run it manually:

```bash
fleetctl prepare db --config /etc/fleet/fleet.yml
```

To target this specific migration in a Go test:

```go
// Apply up to and including this migration
err := migrationClient.Up(db, "20260615135619")
if err != nil {
    log.Fatal(err)
}
```

## Source

[`20260615135619_AddSetupExperienceSoftwareInstallers.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20260615135619_AddSetupExperienceSoftwareInstallers.go)