<!-- source-hash: 8062e912c37ecc9d2c1561537d9a78eb -->
Database migration that transitions label membership storage from the `label_query_executions` table to a dedicated `label_membership` table, and adds a `label_update_time` column to the `hosts` table.

## Key Components

- **`Up20200405120000`** — Applies the migration with four idempotent steps:
  1. Creates the `label_membership` table with composite primary key `(host_id, label_id)`, cascading foreign keys to `hosts` and `labels`, and an index on `label_id`
  2. Migrates matched rows from `label_query_executions` into `label_membership` using `INSERT IGNORE`
  3. Ensures every host is associated with the built-in `All Hosts` label
  4. Drops the legacy `label_query_executions` table and adds `label_update_time` to `hosts` (defaulting to `2000-01-01 00:00:00` for MySQL timestamp compatibility)

- **`Down20200405120000`** — No-op rollback (destructive migration; data loss would occur on rollback)

## Schema Changes

| Action | Target |
|---|---|
| `CREATE TABLE IF NOT EXISTS` | `label_membership` |
| `INSERT IGNORE ... SELECT` | Migrates from `label_query_executions` |
| `INSERT IGNORE ... SELECT` | Populates `All Hosts` membership |
| `DROP TABLE IF EXISTS` | `label_query_executions` |
| `ALTER TABLE ADD COLUMN` | `hosts.label_update_time` |

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up20200405120000, Down20200405120000)

// Apply via migration runner (called externally)
err := MigrationClient.Up(db, "")
```

> **Note:** The `label_update_time` column addition is guarded by `columnExists()` to remain idempotent. The `Down` function is intentionally a no-op — the dropped `label_query_executions` table cannot be safely restored.

**Source:** [`db/migrations/tables/20200405120000_UpdateLabelStorage.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/db/migrations/tables/20200405120000_UpdateLabelStorage.go)