<!-- source-hash: 5b59f313a495fb94dceb17fcdcaf9752 -->
Database migration that creates the `host_disks` table for tracking disk space availability metrics per host.

## Key Components

- **`Up_20220914154915`** — Creates the `host_disks` table with columns for host ID, available disk space in gigabytes, and available disk space as a percentage. Includes an index on `gigs_disk_space_available` for query performance. Uses `CREATE TABLE IF NOT EXISTS` for idempotency.
- **`Down_20220914154915`** — No-op rollback; table removal is not implemented.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Schema

| Column | Type | Notes |
|---|---|---|
| `host_id` | `INT UNSIGNED` | Primary key |
| `gigs_disk_space_available` | `DECIMAL(10,2)` | Defaults to `0` |
| `percent_disk_space_available` | `DECIMAL(10,2)` | Defaults to `0` |
| `created_at` | `TIMESTAMP` | Auto-set on insert |
| `updated_at` | `TIMESTAMP` | Auto-updated on change |

## Usage Example

This migration is automatically registered and executed by the migration client during schema upgrades:

```go
// Applied automatically via MigrationClient
if err := MigrationClient.Up(db, "20220914154915"); err != nil {
    log.Fatalf("migration failed: %v", err)
}
```

**Source:** [`server/datastore/mysql/migrations/tables/20220914154915_CreateHostDisksTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20220914154915_CreateHostDisksTable.go)