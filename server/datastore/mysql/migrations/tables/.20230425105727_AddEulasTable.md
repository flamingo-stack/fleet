<!-- source-hash: 2675144aa35654f432bf0003d7e9ae97 -->
Database migration that creates the `eulas` table for storing End User License Agreement (EULA) records.

## Key Components

- **`Up_20230425105727`** — Creates the `eulas` table with `IF NOT EXISTS` (idempotent). Stores EULA metadata and binary content via a `longblob` column.
- **`Down_20230425105727`** — No-op rollback; the table is left in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

### Table Schema: `eulas`

| Column | Type | Notes |
|---|---|---|
| `id` | `int(10) unsigned` | Primary key |
| `token` | `varchar(36)` | UUID-style identifier |
| `name` | `varchar(255)` | Display name |
| `bytes` | `longblob` | Raw EULA file content |
| `created_at` | `datetime` | Auto-set to `CURRENT_TIMESTAMP` |

Charset: `utf8mb4` / `utf8mb4_unicode_ci`

## Usage Example

```go
// Applied automatically by the migration runner.
// To run manually during development:
if err := MigrationClient.Up(db); err != nil {
    log.Fatalf("migration failed: %v", err)
}
```

## Source

[`20230425105727_AddEulasTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20230425105727_AddEulasTable.go)