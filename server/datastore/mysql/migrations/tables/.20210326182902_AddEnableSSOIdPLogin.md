<!-- source-hash: e9f0fb8fcdab46e51eaeee0a92287d06 -->
Database migration that adds the `enable_sso_idp_login` column to the `app_configs` table, enabling configuration of SSO Identity Provider (IdP) initiated login.

## Key Components

- **`Up_20210326182902`** — Idempotent migration that adds a `tinyint(1)` boolean column `enable_sso_idp_login` (default `0`) to `app_configs`. Uses `columnExists` to skip if already present.
- **`Down_20210326182902`** — No-op rollback; the column is not removed on downgrade.

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20210326182902, Down_20210326182902)
```

The resulting schema change:

```sql
ALTER TABLE `app_configs`
ADD COLUMN `enable_sso_idp_login` tinyint(1) NOT NULL DEFAULT '0';
```

## Notes

- The migration is **idempotent** — safe to run multiple times without error.
- Rollback (`Down_`) is intentionally empty; consider manual cleanup if column removal is required.
- Source: [`20210326182902_AddEnableSSOIdPLogin.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20210326182902_AddEnableSSOIdPLogin.go)