<!-- source-hash: 6db2f6df24cfca8d83a0c90736f8a562 -->
Database migration that drops the `windows_updates` table as part of a schema cleanup operation.

## Key Components

- **`Up_20260423161824`** — Executes `DROP TABLE IF EXISTS windows_updates`, safely removing the table if it exists.
- **`Down_20260423161824`** — No-op rollback; the table is not recreated on down migration.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Applied automatically by the migration runner.
// To run manually via sql.Tx:
func Up_20260423161824(tx *sql.Tx) error {
    if _, err := tx.Exec(`DROP TABLE IF EXISTS windows_updates`); err != nil {
        return fmt.Errorf("drop windows_updates table: %w", err)
    }
    return nil
}
```

> **Note:** The down migration is intentionally a no-op. Rolling back this migration will **not** restore the `windows_updates` table or its data.

## Source

[`20260423161824_DropWindowsUpdatesTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20260423161824_DropWindowsUpdatesTable.go)