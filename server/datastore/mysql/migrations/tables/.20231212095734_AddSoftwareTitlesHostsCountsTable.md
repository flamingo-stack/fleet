<!-- source-hash: dc830ecbfb3ef4319345525a7c3fe2a9 -->
Database migration that creates the `software_titles_host_counts` table, tracking per-team host counts for software titles with timestamp auditing.

## Key Components

- **`Up_20231212095734`** — Creates the `software_titles_host_counts` table using `CREATE TABLE IF NOT EXISTS` (idempotent). Defines a composite primary key on `(software_title_id, team_id)` and two secondary indexes optimized for team/count filtering and timestamp-based lookups.
- **`Down_20231212095734`** — No-op rollback; table removal is not implemented.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Schema

| Column | Type | Notes |
|---|---|---|
| `software_title_id` | `int unsigned` | Part of composite PK |
| `hosts_count` | `int unsigned` | Number of hosts with this title |
| `team_id` | `int unsigned` | Defaults to `0` (global); part of composite PK |
| `created_at` | `timestamp` | Auto-set on insert |
| `updated_at` | `timestamp` | Auto-updated on change |

**Indexes:**
- `PRIMARY KEY (software_title_id, team_id)`
- `idx_software_titles_host_counts_team_counts_title` — supports team-scoped count queries
- `idx_software_titles_host_counts_updated_at_software_title_id` — supports incremental sync queries

## Usage Example

```go
// Applied automatically by the migration runner at startup.
// To run manually using the migration client:
if err := MigrationClient.Up(db, 0); err != nil {
    log.Fatal("migration failed:", err)
}
```

## Source

[`20231212095734_AddSoftwareTitlesHostsCountsTable.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20231212095734_AddSoftwareTitlesHostsCountsTable.go)