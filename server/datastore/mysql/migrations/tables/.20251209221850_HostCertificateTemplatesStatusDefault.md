<!-- source-hash: 82a3851a52cd0b11c9e2803db5a42638 -->
Database migration that modifies the `host_certificate_templates` table by making `fleet_challenge` nullable and adding a `'pending'` default to the `status` column.

## Key Components

- **`Up_20251209221850`** — Applies two idempotent schema changes:
  - Modifies `fleet_challenge` (`char(32)`) to allow `NULL` values, representing records in a pending state before transitioning to delivering.
  - Modifies `status` (`varchar(20)`) to default to `'pending'` when no value is explicitly provided.
- **`Down_20251209221850`** — No-op rollback; schema changes are not reversed.

## Usage Example

This migration is registered automatically via `init()` and executed by the migration client:

```go
// Registered automatically on package import
MigrationClient.AddMigration(Up_20251209221850, Down_20251209221850)
```

The underlying SQL changes applied:

```sql
-- Make fleet_challenge nullable
ALTER TABLE host_certificate_templates
MODIFY COLUMN fleet_challenge char(32) COLLATE utf8mb4_unicode_ci NULL;

-- Add default 'pending' to status column
ALTER TABLE host_certificate_templates
MODIFY COLUMN status varchar(20) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'pending';
```

Both `ALTER` statements are guarded by `columnExists` checks, making the migration safe to re-run.

## Notes

- Migration timestamp: `20251209221850`
- Both column modifications use `utf8mb4_unicode_ci` collation, consistent with Fleet's standard MySQL charset.
- Source: [`server/datastore/mysql/migrations/tables/20251209221850_HostCertificateTemplatesStatusDefault.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20251209221850_HostCertificateTemplatesStatusDefault.go)