<!-- source-hash: f01a74fed09ca4b21840dc18033c9a0f -->
Database migration that adds a `continuous_automations_enabled` boolean column to the `policies` table, with a default value of `0` (disabled).

## Key Components

- **`Up_20260528201150`** — Applies the migration: adds `continuous_automations_enabled TINYINT(1) NOT NULL DEFAULT 0` to `policies` using `ALGORITHM=INSTANT` for a non-blocking schema change. Skips execution if the column already exists.
- **`Down_20260528201150`** — No-op rollback; the column is not dropped on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

```go
// Migration is registered automatically via init().
// To apply manually using the migration client:
err := MigrationClient.Up(db, "20260528201150")
if err != nil {
    log.Fatalf("migration failed: %v", err)
}
```

## Notes

- The `ALGORITHM=INSTANT` directive makes this a metadata-only change on supported MySQL/MariaDB versions, avoiding table rebuilds on large datasets.
- The idempotency check (`columnExists`) ensures the migration is safe to re-run.
- The empty `Down` function means this migration is intentionally irreversible — restore the previous schema manually if a rollback is required.

**Source:** [`20260528201150_AddContinuousAutomationsEnabledToPolicies.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20260528201150_AddContinuousAutomationsEnabledToPolicies.go)