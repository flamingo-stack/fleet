<!-- source-hash: 6bf913c7ae932130477e527b23334678 -->
Database migration that adds a `department` column to the `scim_users` table and registers a new `FLEET_VAR_HOST_END_USER_IDP_DEPARTMENT` fleet variable for exposing SCIM department data to host end-user context.

## Key Components

- **`Up_20250626130239`** — Applies the migration idempotently: adds a nullable `VARCHAR(255)` `department` column (utf8mb4 encoding) to `scim_users` if it doesn't already exist, then inserts the `FLEET_VAR_HOST_END_USER_IDP_DEPARTMENT` entry into `fleet_variables` using `INSERT IGNORE` to avoid duplicates.
- **`Down_20250626130239`** — No-op rollback; the migration is not reversed.
- **`init`** — Registers both migration functions with `MigrationClient` at package load time.

## Usage Example

```go
// Migration is auto-registered via init() and executed by the migration client.
// To apply manually during testing:
err := Up_20250626130239(tx)
if err != nil {
    log.Fatal(err)
}

// Resulting schema change:
// ALTER TABLE scim_users
//   ADD COLUMN department VARCHAR(255)
//     CHARACTER SET utf8mb4
//     COLLATE utf8mb4_unicode_ci
//     DEFAULT NULL;

// Resulting fleet_variables entry:
// name: 'FLEET_VAR_HOST_END_USER_IDP_DEPARTMENT'
// is_prefix: 0
// created_at: 2025-06-27 00:00:00 UTC
```

## Notes

- The `department` column addition is guarded by `columnExists`, making it safe to re-run.
- The `fleet_variables` insert uses a deterministic `created_at` timestamp (`2025-06-27T00:00:00Z`) to ensure generated schema diffs remain stable across environments.
- Source: [`20250626130239_SCIMAddDepartment.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/server/datastore/mysql/migrations/tables/20250626130239_SCIMAddDepartment.go)