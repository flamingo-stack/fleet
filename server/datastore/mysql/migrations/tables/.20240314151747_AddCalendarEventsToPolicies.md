<!-- source-hash: e41b76703a136aebd8199323e58f46c2 -->
Adds a `calendar_events_enabled` column to the `policies` table via an idempotent schema migration.

## Key Components

- **`Up_20240314151747`** — Applies the migration: adds `calendar_events_enabled` (`TINYINT(1) UNSIGNED NOT NULL DEFAULT '0'`) to the `policies` table. Checks column existence before altering to ensure idempotency.
- **`Down_20240314151747`** — No-op rollback; the column is left in place on downgrade.
- **`init`** — Registers both migration functions with `MigrationClient`.

## Usage Example

This migration is applied automatically by the Fleet migration runner. No manual invocation is needed.

```go
// Triggered automatically via MigrationClient on server startup:
MigrationClient.AddMigration(Up_20240314151747, Down_20240314151747)
```

To verify the resulting schema after migration:

```sql
SELECT column_name, column_type, column_default
FROM information_schema.columns
WHERE table_name = 'policies'
  AND column_name = 'calendar_events_enabled';
```

> **Note:** The migration is idempotent — running it multiple times is safe. The `columnExists` guard prevents duplicate `ALTER TABLE` errors in environments where the column may already exist.

**Source:** [`20240314151747_AddCalendarEventsToPolicies.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/20240314151747_AddCalendarEventsToPolicies.go)