<!-- source-hash: 937eee412bfc4a55caf30d969ecd30ca -->
Auto-generated mock implementation of the `fleet.MDMAppleStore` interface, used in tests to stub Apple MDM datastore operations without requiring a real database or MDM server.

## Key Components

**`MDMAppleStore`** — The central mock struct implementing `fleet.MDMAppleStore`. Each method has a corresponding `*Func` field (the injectable stub) and a `*FuncInvoked` boolean (tracks whether the method was called). A `sync.Mutex` ensures thread-safe invocation tracking.

**Function type aliases** — One per interface method (e.g., `StoreAuthenticateFunc`, `EnqueueCommandFunc`), allowing tests to assign custom behavior per operation.

**Covered operations:**
- Device enrollment/authentication: `StoreAuthenticate`, `StoreTokenUpdate`, `Disable`, `StoreUserAuthenticate`
- Command lifecycle: `EnqueueCommand`, `RetrieveNextCommand`, `StoreCommandReport`, `ClearQueue`, `BulkDeleteHostUserCommandsWithoutResults`
- Push notifications: `RetrievePushInfo`, `RetrievePushCert`, `StorePushCert`, `IsPushCertStale`
- Certificate management: `HasCertHash`, `EnrollmentHasCertHash`, `IsCertHashAssociated`, `AssociateCertHash`, `EnrollmentFromHash`
- Bootstrap & secrets: `StoreBootstrapToken`, `RetrieveBootstrapToken`, `ExpandEmbeddedSecrets`, `ExpandHostSecrets`
- Device lock/wipe: `EnqueueDeviceLockCommand`, `EnqueueDeviceUnlockCommand`, `EnqueueDeviceWipeCommand`, `GetPendingLockCommand`
- Config/tokens: `GetAllMDMConfigAssetsByName`, `GetABMTokenByOrgName`, `RetrieveTokenUpdateTally`

## Usage Example

```go
store := &mock.MDMAppleStore{
    EnqueueCommandFunc: func(ctx context.Context, ids []string, cmd *mdm.CommandWithSubtype) (map[string]error, error) {
        return nil, nil
    },
}

// Call the method under test
err := myService.SendCommand(ctx, store, cmd)

// Assert it was invoked
assert.True(t, store.EnqueueCommandFuncInvoked)
```

## Source

[`datastore_mdm_mock.go`](https://github.com/flamingo-stack/fleetmdm/blob/main/datastore_mdm_mock.go)