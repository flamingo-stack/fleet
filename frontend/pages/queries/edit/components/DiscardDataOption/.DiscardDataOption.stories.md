<!-- source-hash: 97b8854fed35286ab56b7cdebe975bbd -->
Storybook story file for the `DiscardDataOption` component, providing a basic story configuration for visual development and testing.

## Key Components

- **`meta`** — Storybook metadata object defining the component title (`Components/DiscardDataOption`) and the target component.
- **`Basic`** — A default story export rendering `DiscardDataOption` with no additional props or configuration.

## Usage Example

```typescript
import { Basic } from "./DiscardDataOption.stories";

// Render the Basic story in a test or Storybook context
<Basic />
```

To add additional stories with custom props:

```typescript
export const WithCustomProps: Story = {
  args: {
    // Pass props specific to DiscardDataOption here
  },
};
```

## Notes

- This file is intended for use within Storybook only and should not be imported in production code.
- Extend the `Basic` story or add new `Story` exports to cover additional prop combinations or states of the `DiscardDataOption` component.
- Source: [`DiscardDataOption.stories.tsx`](https://github.com/flamingo-stack/fleetmdm/blob/main/DiscardDataOption.stories.tsx)