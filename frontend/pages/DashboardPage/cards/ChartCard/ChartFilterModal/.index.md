<!-- source-hash: 735f11dff9fcad84c9981bc8f69e9c17 -->
Barrel export file for the `ChartFilterModal` component, re-exporting the default component and its associated `IChartFilterState` type interface.

## Key Components

- **`default`** — The `ChartFilterModal` component (re-exported from `./ChartFilterModal`)
- **`IChartFilterState`** — TypeScript type interface representing the filter state shape used by the chart filter modal

## Usage Example

```typescript
import ChartFilterModal, { IChartFilterState } from "@/components/ChartFilterModal";

const filterState: IChartFilterState = {
  // filter state properties defined in ChartFilterModal
};

<ChartFilterModal state={filterState} />;
```

## Source

[`index.ts` on GitHub](https://github.com/flamingo-stack/fleetmdm/blob/main/index.ts)