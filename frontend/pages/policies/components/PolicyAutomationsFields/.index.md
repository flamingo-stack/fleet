<!-- source-hash: ebd86434ba108eddbf779a012caf8bac -->
Re-exports the `PolicyAutomationsFields` component and its associated TypeScript types as the public API for the `PolicyAutomationsFields` module.

## Key Components

- **`default`** – The `PolicyAutomationsFields` component (re-exported from `./PolicyAutomationsFields`)
- **`IPolicyAutomationsFieldsHandle`** – Interface type for the component's imperative handle (used with `useRef`/`forwardRef`)
- **`IPolicyAutomationsPayload`** – Interface type representing the payload shape for policy automation field data

## Usage Example

```typescript
import PolicyAutomationsFields, {
  IPolicyAutomationsFieldsHandle,
  IPolicyAutomationsPayload,
} from "components/PolicyAutomationsFields";

const ref = useRef<IPolicyAutomationsFieldsHandle>(null);

const handleSubmit = () => {
  const payload: IPolicyAutomationsPayload = ref.current?.getPayload();
};

<PolicyAutomationsFields ref={ref} />;
```

## Notes

This barrel file provides a clean import path for consumers, hiding the internal file structure. The `IPolicyAutomationsFieldsHandle` type suggests the component exposes an imperative API via `forwardRef`.