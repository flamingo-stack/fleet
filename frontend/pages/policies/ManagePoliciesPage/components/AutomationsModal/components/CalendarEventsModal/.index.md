<!-- source-hash: 78271b7121fcf3d53eaa5c944c63509f -->
Re-exports the `CalendarEventsModal` component and its associated `ICalendarEventsModalData` type from the `CalendarEventsModal` module, serving as the public barrel entry point.

## Key Components

- **`default`** – The `CalendarEventsModal` component (re-exported as default)
- **`ICalendarEventsModalData`** – TypeScript type definition for the modal's data shape

## Usage Example

```typescript
import CalendarEventsModal, { ICalendarEventsModalData } from "./CalendarEventsModal";
```

> Import from the barrel index rather than directly from the implementation file to maintain a clean public API boundary.