<!-- source-hash: 6a053d50d3bb38a74a05d56c7bed1bf9 -->
Storybook stories file for the `OrgLogoIcon` component, providing a default story for visual testing and documentation in the Storybook UI.

## Key Components

- **`meta`** — Storybook metadata configuring the component under `Components/OrgLogoIcon` with no default args
- **`Default`** — Single baseline story rendering `OrgLogoIcon` with no props

## Usage Example

```typescript
// Extend with additional stories as needed
export const WithCustomProps: Story = {
  args: {
    src: "https://example.com/logo.png",
    alt: "Org Logo",
  },
};
```

## Source

[`OrgLogoIcon.stories.tsx`](https://github.com/flamingo-stack/fleetmdm/blob/main/OrgLogoIcon.stories.tsx)