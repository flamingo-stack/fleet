<!-- source-hash: 0659d818c6df0ca13637a57b9d9004ec -->
Storybook stories file for the `TextCell` component, providing a visual development and testing environment for the table text cell component.

## Key Components

- **`meta`** — Storybook metadata configuration registering `TextCell` under `Components/Table/TextCell`
- **`Basic`** — Default story rendering `TextCell` with no props overrides

## Usage Example

```typescript
import { Meta, StoryObj } from "@storybook/react";
import TextCell from ".";

// Add additional stories with custom props
export const WithText: Story = {
  args: {
    value: "Sample cell text",
  },
};
```

## Notes

- Stories are located at [`frontend/components/Table/TextCell/TextCell.stories.tsx`](https://github.com/flamingo-stack/fleetmdm/blob/main/frontend/components/Table/TextCell/TextCell.stories.tsx)
- Run stories via Storybook to preview `TextCell` in isolation
- Extend with additional `Story` exports to cover edge cases (empty values, long text, truncation)