<!-- source-hash: c2b093dd2123b6fd44dcfb6cfcad0340 -->
Re-exports the `FailedEnrollmentProfileModal` component and its TypeScript props interface from the module's internal implementation file, serving as the public barrel entry point.

## Key Components

- **`default`** — Re-exported default export (the `FailedEnrollmentProfileModal` React component)
- **`IFailedEnrollmentProfileModalProps`** — Re-exported TypeScript interface defining the component's prop types

## Usage Example

```typescript
import FailedEnrollmentProfileModal, {
  type IFailedEnrollmentProfileModalProps,
} from "components/FailedEnrollmentProfileModal";
```

## Source

[`index.ts` on GitHub](https://github.com/flamingo-stack/fleetmdm/blob/main/components/FailedEnrollmentProfileModal/index.ts)