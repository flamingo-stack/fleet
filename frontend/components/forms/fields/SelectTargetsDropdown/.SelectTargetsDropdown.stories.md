<!-- source-hash: 6b7dcf84598e23559211e9eabc16f0cf -->
Storybook stories file for the `SelectTargetsDropdown` component, providing interactive UI development and visual testing scenarios.

## Key Components

- **`meta`** — Storybook metadata defining the component, title path (`Components/SelectTargetsDropdown`), and default prop values
- **`Default`** — Baseline story rendering the dropdown with empty targets, disabled state off, and no-op handlers

## Usage Example

```typescript
// Extend with custom story variants
export const WithTargets: Story = {
  args: {
    selectedTargets: [{ id: 1, name: "My Host", type: "host" }],
    targetsCount: 1,
    label: "Select Targets",
    disabled: false,
  },
};

export const Disabled: Story = {
  args: {
    disabled: true,
  },
};
```

## Default Args

| Prop | Default |
|---|---|
| `disabled` | `false` |
| `label` | `"Select Targets"` |
| `selectedTargets` | `[]` |
| `targetsCount` | `0` |
| `queryId` | `1` |
| `onFetchTargets` | `noop` |
| `onSelect` | `noop` |

## Notes

- Uses `@ts-ignore` on the component import — the underlying `SelectTargetsDropdown` module likely lacks TypeScript type exports
- Imports `index.scss` to ensure global styles are applied during Storybook rendering
- Located at `Components/SelectTargetsDropdown` in the Storybook hierarchy

**Source:** [`SelectTargetsDropdown.stories.tsx`](https://github.com/flamingo-stack/fleetmdm/blob/main/SelectTargetsDropdown.stories.tsx)