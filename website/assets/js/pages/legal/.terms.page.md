<!-- source-hash: 7a5a03d8ffe7012f4f20309db29fd4fe -->
Registers the `terms` parasails page component, providing the scaffold for the Terms of Service page with standard Vue-compatible lifecycle hooks and method definitions.

## Key Components

| Member | Type | Description |
|--------|------|-------------|
| `data` | Object | Reactive state properties for the terms page |
| `beforeMount` | Lifecycle Hook | Runs synchronously before the component is mounted |
| `mounted` | Lifecycle Hook | Async hook executed after the component is mounted to the DOM |
| `methods` | Object | Page-specific methods and event handlers |

## Usage Example

```javascript
// Extending the terms page with custom logic
parasails.registerPage('terms', {
  data: {
    lastUpdated: '2024-01-01',
    accepted: false,
  },

  beforeMount: function() {
    // Pre-mount setup, e.g. reading query params
  },

  mounted: async function() {
    // Fetch dynamic content or initialize UI state
    this.lastUpdated = await this.fetchLastUpdated();
  },

  methods: {
    acceptTerms: async function() {
      this.accepted = true;
      // Handle acceptance logic
    }
  }
});
```

## Notes

- Registered under the page key `'terms'`, matched to the corresponding server-rendered view.
- All sections (`data`, `beforeMount`, `mounted`, `methods`) are currently empty placeholders — logic should be added as requirements evolve.
- Source: [`assets/js/pages/legal/terms.page.js`](https://github.com/flamingo-stack/fleetmdm/blob/main/assets/js/pages/legal/terms.page.js)